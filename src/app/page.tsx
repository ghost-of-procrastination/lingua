import { Button } from '@/components/ui/button'

const Home = () => {
	return (
		<div>
			<Button size="lg" variant="destructive" className="cursor-pointer">
				we
			</Button>
		</div>
	)
}

export default Home
